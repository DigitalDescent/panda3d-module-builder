[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

# Project metadata lives in setup.py because the package name is read
# dynamically from config.ini (module_name).
